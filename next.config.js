/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"images.unsplash.com",
			"instagram.com",
			"abaxial.org",
			"studiolotus.in",
		],
	},
};

module.exports = nextConfig

  